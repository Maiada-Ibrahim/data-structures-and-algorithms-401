/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package challenge.app;

import challenge.list.LinkedList;

import static challenge.utilities.StringUtils.join;
import static challenge.utilities.StringUtils.split;
import static challenge.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
